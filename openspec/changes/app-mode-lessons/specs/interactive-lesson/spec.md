# interactive-lesson — delta（app-mode-lessons）

## MODIFIED Requirements

### Requirement: 學員可動手且互相隔離

（已 sync 進 `openspec/specs/interactive-lesson/spec.md`：原「學員可改可跑且互相隔離」
放寬為 edit／app 兩種互動模式，一課只採一種；app 模式禁止出現該模式做不到的操作指示；
兩種模式都保留 `.py` 下載。完整條文與 4 個 scenario 見主 spec。）
