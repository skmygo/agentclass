# Tasks

- [x] `page-fill.py`：`VIDEO` 常數 → 解析 YouTube id → hero `</h1>` 後插 `.video-box`（WRAP 已有則不插）
- [x] llm-apps 十課 `page_content.py` 加 `VIDEO`，重跑 page-fill，diff 只多影片區塊
- [x] 模板註解與 site.md／engineering.md 同步（正規做法＝VIDEO 常數，手放為例外）
- [x] spec 新增「課程影片（可選）」requirement 並 sync
- [x] build + 十課頁面冒煙 + 桌機／手機截圖確認影片區塊位置
